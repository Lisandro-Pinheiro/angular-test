/*
import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { RouterModule } from '@angular/router';
import { AppRoutingModule } from './app-routing.module'; // Ajuste o caminho se necessário
import { AppComponent } from './app.component';
import { PostListComponent } from './components/post-list/post-list.component';
import { PostDetailComponent } from './components/post-detail/post-detail.component';
import { NavigationComponent } from './components/navigation/navigation.component';

@NgModule({
  declarations: [
    AppComponent,
    PostListComponent,
    PostDetailComponent,
    NavigationComponent
  ],
  imports: [
    BrowserModule,
    AppRoutingModule, // Importa o AppRoutingModule para configurar as rotas
    RouterModule // Importa RouterModule para roteamento
  ],
  providers: [],
  bootstrap: [AppComponent] // Inicia a aplicação com o AppComponent
})
export class AppModule { }*/
